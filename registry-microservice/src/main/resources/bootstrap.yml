spring:
    application:
        name: registry-microservice
    cloud:
        config:
            fail-fast: true
            retry:
                maxAttempts: 200
                maxInterval: 10000 
            uri: http://config-microservice:8080
            username: root
            password: s3cr3t