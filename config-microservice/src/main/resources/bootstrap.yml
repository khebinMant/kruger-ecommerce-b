server:
    port: 8080

spring:
    cloud:
        config:
            server:
                git:
                    default-label: development
                    uri: https://github.com/khebinSd/kruger-ecommerce-b
                    searchPaths: config-data
    security:
        user:
            name: root
            password: s3cr3t

