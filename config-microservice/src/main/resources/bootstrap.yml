server:
    port: 8080

spring:
    application:
        name: config-microservice
    cloud:
        config:
            server:
                git:
                    default-label: docker-microservices
                    uri: https://github.com/khebinSd/kruger-ecommerce-b
                    searchPaths: config-data
    security:
        user:
            name: root
            password: s3cr3t

